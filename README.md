# LocalAIChat

LocalAIChat은 C++ 기반 로컬 AI 채팅 프로그램을 단계적으로 만들기 위한 학습용 프로젝트입니다. 최종 목표는 C++ 문법, 프로그램 구조, 자료구조, 메모리, 알고리즘의 기초를 익히면서 작은 콘솔 프로그램을 점진적으로 확장하는 것입니다.

## 현재 단계

0단계: 개발 환경 준비

현재 단계에서는 Visual Studio 2022에서 열고 빌드할 수 있는 네이티브 C++ 콘솔 앱 프로젝트만 준비합니다. 아직 실제 채팅 기능은 구현하지 않습니다.

## 현재 폴더 위치

```text
D:\KM\K82\NA\NA
```

## 프로젝트 파일 구조

```text
D:\KM\K82\NA\NA\
├─ LocalAIChat.sln
├─ README.md
├─ AGENTS.md
├─ .gitignore
└─ LocalAIChat\
   ├─ LocalAIChat.vcxproj
   ├─ LocalAIChat.vcxproj.filters
   └─ main.cpp
```

## Visual Studio 2022에서 여는 방법

1. `D:\KM\K82\NA\NA\LocalAIChat.sln` 파일을 Visual Studio 2022로 엽니다.
2. 솔루션 탐색기에서 `LocalAIChat` 프로젝트와 `main.cpp`가 보이는지 확인합니다.

## 빌드 방법

1. 상단 메뉴에서 `빌드 > 솔루션 빌드`를 선택합니다.
2. 또는 `Ctrl + Shift + B`를 누릅니다.

## 실행 방법

1. 상단 메뉴에서 `디버그 > 디버그하지 않고 시작`을 선택합니다.
2. 또는 `Ctrl + F5`를 누릅니다.
3. 콘솔에 `0단계 실행 확인` 메시지가 출력되는지 확인합니다.

## 디버그 시작 방법

1. 필요한 줄 왼쪽 여백을 클릭해 중단점을 설정합니다.
2. `F5`로 디버그를 시작합니다.
3. `F10`으로 한 줄씩 실행할 수 있습니다.

## 0단계 완료 기준

- Visual Studio 2022에서 솔루션을 열 수 있습니다.
- 솔루션 탐색기에서 `main.cpp`를 확인할 수 있습니다.
- 빌드가 성공합니다.
- 실행하면 콘솔에 `0단계 실행 확인` 메시지가 출력됩니다.

## 아직 구현하지 않은 것

- 채팅 기능
- 사용자 입력
- 반복문 기반 대화 흐름
- `getResponse` 함수
- 자료구조 사용
- 대화 기록 저장
- 파일 입출력
- 실제 AI 모델 연결
- Ollama 연결
- OpenAI API 연결
- 서버 또는 HTTP 요청
- GUI

## 다음 단계 안내

1단계에서는 콘솔 기반 기본 채팅 프로그램을 구현할 예정입니다.

예상 작업:

- 사용자 입력 받기
- `exit` 입력 시 종료하기
- `getResponse` 함수 추가하기
- 간단한 키워드 응답 추가하기

## 출력 또는 입력 글자가 깨질 때 확인할 사항

- Visual Studio 소스 파일 인코딩
- Windows 콘솔 인코딩
- 터미널 글꼴

## 수동 검증 절차

1. `D:\KM\K82\NA\NA\LocalAIChat.sln` 파일을 Visual Studio 2022로 엽니다.
2. 솔루션 탐색기에서 `LocalAIChat` 프로젝트와 `main.cpp`가 보이는지 확인합니다.
3. 상단 메뉴에서 `빌드 > 솔루션 빌드`를 실행합니다.
4. 오류 없이 빌드되는지 확인합니다.
5. `Ctrl + F5`로 실행합니다.
6. 콘솔에 `0단계 실행 확인` 메시지가 출력되는지 확인합니다.